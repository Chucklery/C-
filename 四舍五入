一、利用浮点数向整形自动转换的方式实现四舍五入
  eg 1 :  int a,b;
       scanf("%d",&a);
       b=a*0.2
       if((a*0.2-b))>=5) 
          b++;
  eg 2 :b=a*0.2+0.5;--> 可直接实现四舍五入  
二、向上向下取整函数（首先定义#include<math.h>）
  向上取整ceil（变量或者常量）；
  向下取整floor(变量或者常量)；
    
